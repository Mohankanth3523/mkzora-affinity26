/**
 * DEPRECATED — Phase 11 replaced this component with
 * `RegistrationProgress.tsx` (step indicator) and
 * `RegistrationNavigation.tsx` (Back/Next). `app/register/page.tsx` no
 * longer imports this file — confirmed no other file does either.
 *
 * This file is a stub rather than fully removed because this session's
 * device bridge can write files but not delete them; please delete
 * `components/registration/WizardNav.tsx` by hand next time you're in
 * this folder. See docs/phase-11-registration-architecture-notes.md.
 */
export {};
